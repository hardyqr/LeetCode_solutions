class Solution(object):
    def findSubstring(self, s, words):
        """
        :type s: str
        :type words: List[str]
        :rtype: List[int]
        """
        # how to ergodic all combinations of orders in this list?
        length=len(words)
        for i in range(1,length):
            aws[i]=

        for word in words:

            aws.append()
        return aws
